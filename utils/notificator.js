export const smsSenderAndReceiver = (senderPhone, receiverPhone, packCode) => {
  console.log(
    `Pack code ${packCode} sent to senderPhone ${senderPhone}, and receiverPhone ${receiverPhone}`
  );
};
